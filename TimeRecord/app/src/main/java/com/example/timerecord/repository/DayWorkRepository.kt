package com.example.timerecord.repository

import com.example.timerecord.data.Work
import java.util.Date

class DayWorkRepository {
    
    fun getWorks(selectedDate : Date) : ArrayList<Work> {
        TODO("데이터 베이스에서 ${selectedDate} 일정 가져오기 ")
    }
}